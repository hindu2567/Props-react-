PROPS☹(properties) it is an object stores data in object
Props in react are special objects used to pass data from one component to another, typically from a parent component down to its child components.
